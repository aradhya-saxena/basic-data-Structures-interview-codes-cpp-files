#include <algorithm>
#include <string>
#include <iostream>
#include <map>
using namespace std;
struct list1{
        int v;
        list1 * next;
    };
struct list2{
        int v;
        list2 *next;
};
struct list 3 {

    int v;
    list3 * next;
};

int func1(list1 * ptr1, list2 * ptr2, int carry){

    if(ptr->next==NULL )

    int digit= ptr1->v+ptr2->v+carry;
    list3 ptr3 = (list3*) malloc (sizeof(lits3));
    if(digit<=9){


        func1(ptr1->next,ptr2->next,0)

    }


}


int main(){


map <int,int> mp =
    int i=0;
    node * ptr= (node *)malloc(sizeof(node));
    while(i<5){
        ptr->v= i;
        ptr->next= (node *)malloc(sizeof(node));
        cout<<ptr->v<<endl;
        i++;
    }

return 0;
}
